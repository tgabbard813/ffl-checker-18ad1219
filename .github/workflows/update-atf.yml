name: Update ATF FFL List (disabled)

# Auto-update is disabled. public/ffl-list.csv is maintained manually now —
# replace it in the repo and the app will load the new file.
# To re-enable, restore the cron schedule.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Auto-update disabled. Replace public/ffl-list.csv manually."
